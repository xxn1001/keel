# 更新载荷里的 ESP —— 我们**不**使用这个分区,只保留定义。
#
# 原因:mkosi 在 Bootable=yes 时会往镜像树的 /boot 装 systemd-boot 与 UKI,
# 而 repart 需要一个 ESP 定义来承载它。载荷真正需要的是 SplitArtifacts 抽出来的
# keel-slot-a.efi(单独一个文件),这个分区镜像会被忽略。
[Partition]
Type=esp
Format=vfat
Label=esp
CopyFiles=/boot:/
SizeMinBytes=1G
SizeMaxBytes=1G
