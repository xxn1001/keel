# B 槽载荷的根分区
#
# 类型与安装镜像里的 B 槽保持一致(linux-generic,不是 root 类型),
# 标签必须是 root-b:UKI 的 cmdline 里写的是 root=PARTLABEL=root-b。
[Partition]
Type=linux-generic
Format=ext4
Label=root-b
CopyFiles=/
SizeMinBytes=6G
SizeMaxBytes=6G
SplitName=root-b
