# 产物形态:B 槽更新载荷
#
# 和 slot-a 完全对称,只是分区标签与 cmdline 换成 root-b。
# 注意:两份 UKI 的 cmdline 不同(各自指向自己的槽),所以不能互相复制 ——
# 这是"每槽一个完整 UKI"的必然结果,也是回滚时内核与根永远配对的原因(不变量 3)。
[Output]
RepartDirectories=repart/slot-b
Output=keel-slot-b

[Content]
KernelCommandLine=root=PARTLABEL=root-b
